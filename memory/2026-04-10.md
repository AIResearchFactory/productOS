- User rejected the `airesearchfactory-site` productOS homepage redesign as looking bad and asked to deprecate that work; branch involved was `site/productos-homepage`.
- productOS strategic direction shifted toward a browser-first migration with no desktop requirement, no OS signing dependency, and npm as the primary toolchain.
- Created migration brief for review/share: `C:\Users\User\.openclaw\workspace\products\productos-migration-brief.md`.
- User asked to start the migration based on that brief by rebasing to `main`, creating a fresh branch from it, and beginning implementation.
- Reported working branch for migration start: `migration/browser-runtime-foundation`.
## 2026-04-10 browser-first migration progress
- Continued work in `C:\Users\User\.openclaw\workspace\repos\productOS` on the browser-first/local-runtime migration, not the rejected website redesign direction.
- Expanded `src\api\runtime.ts` to provide browser/localStorage-backed support for project files and artifacts, including `getProjectFiles`, `readMarkdownFile`, `writeMarkdownFile`, `renameFile`, `deleteMarkdownFile`, `listArtifacts`, `createArtifact`, `saveArtifact`, and `deleteArtifact`.
- Kept the migration policy explicit: use the shared `appApi` layer for browser-first slices, keep native-only behavior behind `isTauriRuntime()`, do not fake unsupported native side effects in browser mode, and verify changes with real builds.
- Migrated first workspace CRUD slice in `src\pages\Workspace.tsx` from direct `tauriApi` usage to shared `appApi` for document/file/artifact operations.
- Migrated a safe `ChatPanel.tsx` slice to shared/runtime-aware behavior for settings load, provider bootstrap, project file refresh, artifact refresh, and provider persistence; build passed after the change.
- Migrated the core `MarkdownEditor.tsx` document loop to shared API paths for document load, autosave-on-switch, manual save, and project settings reads for PPTX export; kept true native-only extras such as artifact metadata updates on Tauri paths; build passed.
- Migrated a first safe `GlobalSettings.tsx` slice to shared/runtime-aware paths for global settings load/save, provider detection, app version, and projects list, while gating native-only auth status, usage stats, and related capabilities to Tauri; build passed.
- Repeated guiding recommendation at end of the session: finish remaining `GlobalSettings` browser/runtime gating next, then continue into deeper chat/workflow migration.
- Continued productOS browser-first/local-runtime migration in `repos/productOS` with QA-guided cleanup focused on runtime-visible browser issues.
- Confirmed working policy split: browser-safe/shared behavior goes through `appApi`; true native-only behavior stays behind Tauri/runtime checks and is not faked.
- Completed browser-safe workflow CRUD + schedule/history storage in `src/api/runtime.ts` and switched corresponding `Workspace.tsx` workflow create/edit/delete/save/schedule flows to shared API, while keeping real workflow execution Tauri-only with honest browser toast messaging.
- Completed browser-safe search/replace-in-files support: added `searchInFiles` and `replaceInFiles` to `src/api/runtime.ts`, switched `Workspace.tsx` search/replace paths to `appApi`, and verified with successful build.
- Completed another migration slice for chat/browser behavior: in `ChatPanel.tsx`, moved chat-created workflow save/refresh, workflow scheduling/unscheduling, and chat file-reference reads to `appApi`; in hooks, moved `useAiCompletion.ts` and `useWorkflowGenerator.ts` to shared API.
- Added browser-runtime `getCompletion(...)` fallback in `src/api/runtime.ts` after a build exposed the missing shared API method.
- Verified the latest slice with `npm run build` passing after fixing the `getCompletion` gap.
- Committed latest productOS changes in repo `repos/productOS` with commit `fa76977` and message `Expand browser-safe chat and workflow APIs`.
- Current rough migration progress estimate communicated to user: about 68%.
- Remaining notable work areas: native event-driven chat plumbing, real workflow execution/progress lifecycle, provider switching and usage stats paths, install/import flows (skills/MCP/import-export/artifact import), update/install/relaunch flows, and remaining native-only `Workspace.tsx` integrations plus build warning cleanup.
