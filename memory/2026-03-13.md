# Memory Log - 2026-03-13
## 2026-03-13 15:43 (Asia/Jerusalem) — Session durable summary

- productOS security fix implemented for Gemini CLI auth command injection in `src-tauri/src/commands/settings_commands.rs` and committed (`54979a8`).
- productOS workflow generation bug fixed to prioritize project-created skills over imported skills; patch landed and pushed to PR branch (`f970f53`, later `c34caca` on PR #82 branch `fix/workflow-creation-logic`).
- PR #82 review findings and follow-up patches:
  - release tag creation race in matrix fixed by introducing single `create-tag` job and making matrix release depend on it.
  - release-notes script hardened by replacing shell-interpolated `execSync` with `spawnSync` + ref validation.
  - fixes committed/pushed (`26fb06b`) to PR branch.
- Created productOS documentation cookbook and demo assets:
  - `docs/11-cookbook-use-cases.md` with practical PM recipes and prompts.
  - `docs/demo-pack/*` including cases, workflow JSON, captions, simulation script, and Remotion scenes.
  - generated outputs: case stills + `demo-pack.mp4` under `docs/demo-pack/remotion/out/`.
  - one-click runners added: `run-demo-pack.ps1` (Windows) and `run-demo-pack.sh` (macOS/Linux) + macOS guide `docs/demo-pack/MACOS.md`.
  - related commits include `b462525`, `a7d3be0`, `32b3d12`.
- New feature branch created and implemented: `feature/opencode-cli-provider`.
  - Added first-class `openCodeCli` provider across backend+frontend (models, settings defaults, provider runtime, AI service wiring, UI dropdowns, TS types).
  - Validation passed (`npm run build`, `cargo check`).
  - commit: `5a98e5a`.

- External repo security work completed locally on `everything-claude-code`:
  - hardened `.opencode/tools/git-summary.ts` against command injection,
  - tightened auto-approve regex/security in `.opencode/plugins/ecc-hooks.ts`,
  - improved safety wording in migration/prompt docs.
  - commit on local branch: `6fcad78` (`security/harden-opencode-skills`), push blocked due to missing fork permissions.
