---
description: Build or Run the productOS application locally
---

# Run Locally (Development)
To run the application in development mode (starts Vite and Node server):
// turbo
1. npm run dev

# Build (Production)
To build the frontend and prepare the Node backend:
// turbo
1. npm run build

# Run Production
To run the compiled application locally:
// turbo
1. npm start
